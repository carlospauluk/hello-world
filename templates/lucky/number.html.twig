{# templates/lucky/number.html.twig #}
<body>
	<h1>
		Your lucky number is
		{{ number }}
	</h1>

	{% for i in 1..10 %}
	<div class="{{ cycle(['even', 'odd'], i) }}">
		{{ i }}
	</div>
	{% endfor %}

</body>